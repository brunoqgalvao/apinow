{
  "name": "apinow",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:generate": "pnpm --filter @apinow/api db:generate",
    "db:migrate": "pnpm --filter @apinow/api db:migrate",
    "db:seed": "pnpm --filter @apinow/api db:seed"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0"
}
